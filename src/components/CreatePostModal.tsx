'use client';

/**
 * Legacy re-export — PostComposer is now the single inline composer.
 * Keep this file so any old imports still resolve without a modal popup.
 */
export { default } from '@/components/PostComposer';
